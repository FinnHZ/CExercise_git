#include <stdio.h>
#include <conio.h>
#include <math.h>


void main(){
    int a, b, c;
    a = 1 + 5 > 2 * 4;
    b = 'A' < 'B';
    c = a == b;
    printf("a=%d, b= %d, c=%d\n",a, b, c);

    // if(a > b) c = a;
    // printf("")

}